# .github/secret_scanning.yml
patterns:
  - name: "NABKISAN API Keys"
    regex: "NKFL_[A-Za-z0-9]{32}"
  - name: "Database Connection Strings"
    regex: "Data Source=.*Password=[^;]*"
  - name: "Azure Storage Keys"
    regex: "DefaultEndpointsProtocol=https.*AccountKey=[A-Za-z0-9+/]+=*"
